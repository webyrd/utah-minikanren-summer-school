#lang racket
(require (only-in "mk.rkt" run run*
                  == =/=
                  fresh eigen
                  conde conda condu
                  symbolo numbero ;; not-pairo
                  absento
                  prt project))

(provide (all-defined-out))

(include "numbers.scm")


